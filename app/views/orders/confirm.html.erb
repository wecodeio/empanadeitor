<h1>¡Casi estamos!</h1>
<h4>Una vez que te hayas comunicado con el local de comidas, carga el precio para saber cuánto deberá pagar cada
  integrante</h4>
<div class="row">
  <div class="col">
    <h3>Datos del comercio</h3>
    <ul>
      <li><%= @order.place_name %></li>
      <% if @order.place_phone %>
        <li>Teléfono: <%= @order.place_phone %></li>
      <% end %>
    </ul>
  </div>
</div>
<table class="table table-striped table-responsive">
  <%= render partial: 'summary' %>
  <tfoot>
  <tr>
    <td id="column"><strong>Total de empanadas</strong></td>
    <td id="column"><strong><%= @order.total_units %></strong></td>
    <% @order.list_of_participants.each do |person| %>
      <td id="column"><%= @order.total_units_per_person[person] %></td>
    <% end %>
  </tr>
  </tfoot>
</table>
<div class="row">
  <div class="col-md-5">
    <%= form_for :order_data, url: finish_order_path do |f| %>
      <div class="form-group">
        <p><strong>¿Cuál es el costo del pedido?</strong></p>
        <div class="input-group mb-3">
          <div class="input-group-prepend">
            <span class="input-group-text">$</span>
          </div>
          <%= f.text_field 'price', class: 'form-control' %>
        </div>
      </div>
      <%= f.submit 'Ya ordené el pedido', class: 'btn btn-primary orden-resuelta' %>
      <%= f.submit 'Quiero volver a editar', class: 'btn btn-primary editar-orden' %>
    <% end %>
  </div>
</div>
